import Vue from "vue";

export const Home = Vue.component('home', require('./home.vue').default);
export const Dashboard = Vue.component('dashboard', require('./dashboard-login.vue').default);
export const Profile = Vue.component('profile', require('./profile.vue').default);
export const Twobind = Vue.component('twobind', require('./twobind.vue').default);
